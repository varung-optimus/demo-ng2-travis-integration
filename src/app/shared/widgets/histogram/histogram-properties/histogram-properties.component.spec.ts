/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HistogramPropertiesComponent } from './histogram-properties.component';

describe('Component: HistogramProperties', () => {
  it('should create an instance', () => {
    let component = new HistogramPropertiesComponent();
    expect(component).toBeTruthy();
  });
});
