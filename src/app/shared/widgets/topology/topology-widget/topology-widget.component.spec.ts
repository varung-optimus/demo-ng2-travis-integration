/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TopologyWidgetComponent } from './topology-widget.component';

describe('Component: TopologyWidget', () => {
  it('should create an instance', () => {
    let component = new TopologyWidgetComponent();
    expect(component).toBeTruthy();
  });
});
