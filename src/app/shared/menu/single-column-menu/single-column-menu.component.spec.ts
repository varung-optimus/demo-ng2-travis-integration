/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SingleColumnMenuComponent } from './single-column-menu.component';

describe('Component: SingleColumnMenu', () => {
  it('should create an instance', () => {
    let component = new SingleColumnMenuComponent();
    expect(component).toBeTruthy();
  });
});
