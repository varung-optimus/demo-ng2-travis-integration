/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MultiColumnMenuComponent } from './multi-column-menu.component';

describe('Component: MultiColumnMenu', () => {
  it('should create an instance', () => {
    let component = new MultiColumnMenuComponent();
    expect(component).toBeTruthy();
  });
});
